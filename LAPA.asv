clear

%Boundary conditions
LBC = 1;
RBC = 0;

nx = 2;
ny = 2;
V = (nx+1,ny+1);
maxiterations = 5;

V(1,1) = 1;
V(1,2) = 1;
V(1,3) = 1;

for iter = 1:1:maxiterations
    for n=2:1:nx
        for m=2:1:ny
            V(m,n)=(V(m+1,n)+V(m-1,n)+V(m,n+1)+V(m,n-1))/4;
        end
    end
end